import kotlin.math.pow

// You can experiment here, it won’t be checked

fun main(args: Array<String>) {
  // put your code here
    val x = readLine()!!.toDouble()
    print(x.pow(3.0) + x.pow(2.0) + x + 1)
}
